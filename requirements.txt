# Core backend
fastapi>=0.110,<1
uvicorn[standard]>=0.30

# Provider-agnostic LLM (any provider; no OpenAI SDK lock-in). Optional: only needed for
# AI-written fields. The deterministic path (field placeholders + credential report) works without it.
litellm>=1.83.0

# Dev/test
pytest>=8
httpx>=0.27
